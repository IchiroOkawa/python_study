def plus(a,b):
    """Exmaple function with type documented in the docstring.
    
    Args:
        a (int) : The first parameter.
        b (int) : The second parameter.

    Returns:
        c (int) : The return value. c = a + b
    """
    c = a + b
    return c
